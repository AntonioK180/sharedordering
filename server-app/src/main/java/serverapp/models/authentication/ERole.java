package serverapp.models.authentication;

/**
 * Describes the supported roles, although actually at the moment
 * only the ROLE_USER is used.
 */
public enum ERole {
    ROLE_USER,
    ROLE_MODERATOR,
    ROLE_ADMIN
}
